version: '3.8'

services:
  proxy:
    image: ghcr.io/yaqiangsun/qubitproxy:latest
    pull_policy: always
    restart: unless-stopped
    ports:
      - "9801:8000"
    volumes:
      - ./tmp:/app/tmp
      - ./config:/app/config